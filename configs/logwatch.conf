# Logwatch Configuration for Rewind-OS Security Monitoring

# Output settings
Output = mail
Format = html
Encode = none

# Email settings
MailTo = root@localhost
MailFrom = Logwatch-Rewind-OS

# Time range
Range = yesterday

# Detail level (Low, Med, High, or number from 0-10)
Detail = High

# Services to monitor
Service = All
Service = zz-network
Service = zz-sys
Service = iptables
Service = kernel
Service = pam_unix
Service = sshd
Service = su
Service = sudo

# Rewind-OS specific log monitoring
# This would be extended to monitor Rewind-OS specific logs

# Archive settings
Archives = Yes

# Numeric IP addresses
numeric = Yes